# 经历工作台 · 方案文档

> 版本 v1.0 ｜ 2026-09-15 ｜ 状态：已确认，进入实现

---

## 0. 一句话定位

**一个只跑在你自己电脑上的竞赛台账：不让你错过任何截止，让你找得到任何材料，让你说得清自己做过什么。**

它由三件东西组成：**台账**（往里录）+ **提醒器**（替你看时间）+ **成果生成器**（往外给）。

**明确不做**：天气、课表、待办清单、笔记、番茄钟。凡是不能服务于"不错过 / 找得到 / 说得清"这三件事的模块，都不进这个工具。

---

## 1. 目标与边界

### 1.1 要解决的四个痛点

| 痛点 | 对应模块 | 验收标准 |
|---|---|---|
| 节点不遗漏 | 节点赛程 + 提醒器 | 提前 7/3/1 天三次提醒，漏发开机补齐 |
| 材料可复用 | 材料库 | 报名材料可存为模板，年年复用 |
| 成果能输出 | 成果导出 | 一键生成保研/求学/出国三种口径 |
| 复盘有沉淀 | 复盘笔记 | 每次结束后沉淀，形成越用越厚的经验库 |

### 1.2 使用规模假设

同时跟进 **3–5 项**竞赛。这个规模决定了：**不做表格化高密度界面，也不做过度留白的展示型界面**，走中等密度卡片 + 看板。

### 1.3 已确认的硬约束

| 项 | 决定 |
|---|---|
| 数据存放 | **纯本地**，不经过任何服务器 |
| 运行形态 | **本地小服务**（双击启动，浏览器作界面，数据是本地真实文件） |
| 数据模型 | 抽象成**「经历」**，用类型区分 |
| 专业 | 支持**多专业**（双学位/辅修） |
| 鉴权 | **后置**，当前只监听本机 `127.0.0.1` |
| 手机端 | 后期只读 + 接收通知，**现在只预留接口** |
| 视觉 | 用户提供模板；模板到达前按本方案 §7 的基调执行 |

---

## 2. 数据模型

### 2.1 核心抽象：一切皆「经历」

竞赛不是一种特殊的实体，它只是**经历的一种类型**。这样建模的原因是：你最终要交出去的材料（简历、保研表）本来就是混着写的，早点统一模型最省事，以后加第四种口径不用改数据结构。

```
经历 Experience
├─ 类型 type：竞赛 / 项目 / 实习 / 论文 / 奖项
├─ 基本信息：名称、级别、赛道、角色、组织方、起止时间、状态
├─ 节点 nodes[]      ← 时间点，状态机的主线
├─ 材料 materials[]  ← 附件引用
├─ 关联 links[]      ← 谁带队、和哪些经历相关
└─ 复盘 retro        ← 结束后填写
```

### 2.2 节点是主线，不是附属

一个竞赛的价值不在"它叫什么"，而在"它现在到哪一步了、下一步是什么时候"。所以节点是一等公民：

| 字段 | 说明 |
|---|---|
| `title` | 节点名（报名截止 / 作品提交 / 答辩 …） |
| `kind` | 提醒 / 提交 / 答辩 / 结果 / 其他 |
| `dueAt` | 截止时间（**唯一必填项**，且必须精确到时刻） |
| `done` | 是否已完成 |
| `remindDays` | 覆盖默认的 7/3/1（特殊节点可单独设） |
| `notifiedAt` | 已发出的提醒记录（用于补发判断） |

**赛程模板**：选中某个预置竞赛后，自动套用它的大致赛程（如数模 = 公布赛题 → 提交 → 答辩），日期留空由你填。这样就实现了"录入成本最低"。

### 2.3 字段清单

**profile.json — 你的设置**

| 字段 | 说明 |
|---|---|
| `majors[]` | 专业（可多个），用于竞赛推荐排序 |
| `grade` | 年级 |
| `school` | 学校 |
| `remind.time` | 每日扫描时间，默认 `08:00` |
| `remind.days` | 提前量，默认 `[7,3,1]` |
| `remind.channels[]` | 送达渠道：`desktop` / `email` |
| `remind.smtp` | 邮箱配置（不填写则降级为落盘 + 桌面通知） |
| `export.profile` | 当前导出默认口径 |

**library.json — 预置竞赛库**（见 §5）

**experiences.json / awards 一并**：所有经历，结构见 2.1

### 2.4 为手机端预留的四个字段（现在做几乎零成本，将来补是灾难）

| 字段 | 用途 |
|---|---|
| 主键用 **UUID** | 不用自增序号，避免多端撞号 |
| `updatedAt` + `deviceId` | 为字段级合并（LWW）做准备 |
| `deletedAt` | **软删除**，否则同步时另一端不知道要删什么 |
| `changelog.jsonl` | 变更日志，手机端同步时只拉增量 |

> **注**：你描述的"电脑开机同步 + 手机只读"本质是**单向推送**，将来**不需要 CRDT**。别被 local-first 那套复杂度带跑。

---

## 3. 信息架构

### 3.1 首页（Bento 网格）

不用一列卡片，用**大小格拼接**——这是当下 dashboard 的主流做法，特别适合内容类型混杂的首页。

```
┌───────────────────────┬───────────────┐
│  本周三件事            │  在研竞赛 4    │
│  （最大的格子）         │  ───────────  │
│  最紧急的一条带倒计时    │  临近节点 7    │
│  其余两条压在其下       │  已获奖项 3    │
├───────────┬───────────┼───────────────┤
│ 即将开始    │ 最近复盘   │  材料待补 2    │
│ 的竞赛      │ （一句话） │               │
└───────────┴───────────┴───────────────┘
```

**为什么"本周三件事"占最大格**：这个工具唯一不可替代的价值是防漏截止。其他信息都可以退后。

### 3.2 六个视图

| 视图 | 作用 | 关键交互 |
|---|---|---|
| **概览** | 一屏回答"我现在什么状况" | Bento 网格；点格子钻进去 |
| **经历档案** | 每项经历一张卡 | **看板拖拽改阶段** / 表格批量筛选 / **卡片侧滑看详情（不跳页）** |
| **节点赛程** | 所有经历的时间点摊到一条时间轴上 | 跨竞赛**冲突检测**（同一天两个截止会标出来） |
| **材料库** | 证书、作品、报名表集中存 | **可复用模板**（报名材料年年重复填，抽成模板最省力） |
| **复盘笔记** | 每次结束后记"做对了什么 / 踩了什么坑 / 哪些材料可复用" | 结构化三栏，不是自由文本框 |
| **成果导出** | 一键生成三种口径 | 选口径 → 预览 → 复制 / 导出 |

### 3.3 借鉴来源（已调研）

开源竞赛管理系统（GitHub 上那批 SpringBoot/Vue 项目）**全部是学校管理员视角**——报名审核、成绩录入、角色权限。信息结构与我们完全反向，**功能一个都不能抄，但信息结构可以抄**。

真正同构的是**求职者的"投递追踪器"**：在跟 N 个机会、每个有若干阶段和时间点、最关心"下一个动作"。四个交互直接借鉴：

1. **看板拖拽改阶段**（`job-application-tracker`）
2. **卡片侧滑看详情，不跳页**（`personal-job-board`）
3. **Journey Map**——用一条 SVG 时间线画出该项经历的状态变迁（`personal-job-board`）
4. **Focus Mode**——只看高优先级的（`personal-job-board`）

---

## 4. 提醒机制（A 方案：本地开机补发）

### 4.1 运转流程

```
电脑开机 → 小服务自动启动
   ↓
每天 08:00 扫描未来 7 天的节点
   ↓
找出「该提醒但还没发过」的   ← 这一步就是补发
   ↓
汇总成一封，发到你的邮箱 / 弹桌面通知
```

**补发的含义**：你三天没开机，第 4 天开机时它会把这几天的提醒合并补给你。判断依据是节点上的 `notifiedAt`——发过的不会重发，没发过的一定会补。

### 4.2 三次提醒，三种措辞

| 提前量 | 语气 | 目的 |
|---|---|---|
| 7 天 | "有件事来了" | 让你心里有数，有时间安排 |
| 3 天 | "该准备了" | 让你动手 |
| 1 天 | "今天必须交" | 最后一道防线 |

**逾期后**：工具里标红，但**不再发邮件**。理由：逾期后的邮件只会让你想关掉提醒。

### 4.3 送达渠道

| 渠道 | 状态 | 说明 |
|---|---|---|
| Windows 桌面通知 | 默认开启 | 不依赖任何配置，走系统 API |
| 邮件 | 需配置 | 填了 SMTP 才启用；**不填则自动降级**，把邮件落成 `.eml` 文件放在 `data/outbox/` |
| 云端代收（B 方案） | **不做** | 接口预留，赛前关键期可再议 |

### 4.4 必须知道的两个限制

1. **电脑关机期间收不到提醒**。所以提前量必须给足（7 天起），只要你一周内开过机就不可能漏。
2. **邮件只能提醒，不能操作**。邮件里的链接打不开你家里的电脑，所以"是否参加"这类确认动作，只能在电脑上打开工具时做。若将来要在被窝里点一下就确认，那必须回到 B 方案。

---

## 5. 预置竞赛库

### 5.1 骨架来源

**中国高等教育学会《全国普通高校大学生竞赛分析报告》榜单，共 84 项**——这是国内各高校保研 / 综测加分认定的基准。

这个选择带来三个现成的好处，不用自己判断：

1. **含金量**已由官方排序，我们照搬；
2. **"专业 → 竞赛"的推荐关系是现成的**——榜单本身分了综合创新 / 理工 / 经管 / 文法 / 艺术 / 机器人 / 医学 / 能源环境 / 测绘地理 / 金融商业 / 语言文化 / 国际赛事等类；
3. **时间窗口是现成的**（"通常 X 月报名、Y 月开展"）。

### 5.2 两层结构：稳定层预置，易变层你自己填

**这是本方案里最重要的一条设计决策。**

| 层 | 内容 | 是否预置 | 为什么 |
|---|---|---|---|
| **稳定层** | 全称、主办方、类别、官网、大致时间窗口、常见赛道 | ✅ 预置 | 年年不变，可放心预置 |
| **易变层** | 当届精确截止日期、具体赛程、费用 | ❌ 不预置 | 每年都变 |

**为什么易变层坚决不预置**：一个**错的截止日期比没有截止日期更危险**——你会信它，然后错过。所以当届信息一律由你填，或者从通知里粘贴。

### 5.3 库的版本机制

库里带两个字段：

- `libVersion`：库的版本号
- `dataAsOf`：数据截止日期

**流程层 vs 数据层，两件事都要有**：

- **流程层**：赛前提醒"要不要参加" → 你确认 → 生成"我在跟这个竞赛"的记录 → 你补当届截止日。这是**动态的、你说了算的**。
- **数据层**：库里那些"稳定信息"也会变——某竞赛换了主办方、改了赛道、从省级升成国家级。**没有任何提醒会告诉你这件事**。所以库必须能整体换代（导入更新包），否则第三年你翻开的是三年前的旧档案。

一句话：**流程靠手动确认，数据靠版本换代。**

### 5.4 推荐规则

按三类信号加权排序：

1. **专业相关度**（榜单类别 ↔ 你的专业标签）
2. **级别**（榜单内 / 榜单外观察目录）
3. **时间窗口**（未来 90 天内可报名的排前面）

多专业时取并集，同一条竞赛命中多个专业就提权。

---

## 6. 成果导出

### 6.1 三套口径 × 中英双语

| 口径 | 重什么 | 输出 |
|---|---|---|
| **保研** | 级别、名次、排名占比、指导老师 | 中文 |
| **求学**（考研/申请） | 专业能力、你具体做了什么、可验证成果 | 中文 |
| **出国** | 动词开头的成果描述、量化结果、国际可比性 | **中英双语**（原始记录是中文，CV/PS 通常要英文） |

### 6.2 实现原则：数据与样式分离

**一份数据，三套渲染规则。** 数据结构不动，只换排版和措辞规则——以后加第四种口径，一行数据结构都不用改。

（这一条来自对 JSON Resume / RenderCV / Reactive Resume 的调研：这些项目值得抄的不是模板长相，而是"内容与样式分离"这个结构。）

### 6.3 输出格式

先出 **Markdown + HTML**——能复制、能转 Word、能转 PDF。**不上 LaTeX**：那是技术炫技，对交材料没有帮助。

---

## 7. 视觉与动效

### 7.1 基调（在用户模板到达前执行）

- **Bento 网格**：大小格拼接，适合内容类型混杂的首页。
- **功能性微动效**：2026 年的趋势已从"加动效让它好看"转向"动效得帮上忙"。参考 iOS 下拉刷新那种**跟手**感——比十个飘来飘去的粒子更显质感。
- **动效只放三处**：列表项进入 / 节点勾选完成 / 保存成功。**不铺满**。
- **主动排除**：Neo-Brutalism（粗黑边、高饱和、故意不精致）和全息渐变那一挂——确实很 Gen Z，但用在工具上两周就嫌吵。这个工作台要陪你四年，选耐看的。
- **强调色不用蓝**：蓝太像学校教务系统。

### 7.2 关于 Three.js / ThreeUI 的结论

**ThreeUI = Three.js 的 3D 视觉组件库**（不是 shadcn 那种通用组件库）。它的分类是 Landing Pages / Hero / Backgrounds / Text Animation——**全是营销页取向**。

- **当基座：不行。** 本工具 90% 的界面是表格、日期选择、筛选、空状态、批量操作，它一个都不提供。
- **当点缀：可以。** 只有两处值得——首页的**成果墙**、以及**空状态**（还没录任何竞赛时，一个缓慢转动的 3D 形状比"暂无数据"体面得多）。
- **三条纪律**：懒加载 / 可关闭（给"减少动效"开关）/ 不参与信息表达。
- **已知的坑**：部分组件会渲染整份 HTML 文档、依赖固定根路径资源，不是 drop-in，接入时得单独搬资源。

引用 Three.js 的官方定位作为判断依据：它适合"轻量级 3D 展示、IoT 看板、教育演示"，短板是包体积大、破坏性变更频繁、移动端性能紧张。**"大多数业务数据用精心设计的 2D 仪表盘表现更好；3D 只在第三维真的承载信息时才值得。"** 竞赛数据没有空间结构，所以 3D 只配装饰。

---

## 8. 技术选型

### 8.1 选型结论

| 层 | 选择 | 理由 |
|---|---|---|
| 运行 | **Node.js 本地小服务** | 已在机器上可用；生态成熟；写文件、发邮件、起定时任务都顺 |
| 数据 | **本地 JSON 文件** | 你的规模（3–5 项、几十条记录）远不到需要数据库；JSON 可读、可手工改、可 diff、备份＝复制文件 |
| 前端 | **原生 HTML/CSS/JS，零构建** | 双击就能跑，不需要 `npm run build`、不需要装环境。这类工具最容易死在"启动太麻烦"上 |
| 动效 | **CSS transition + View Transitions API** | 原生支持"卡片从列表展开成详情"的连贯变形——这正是想要的效果，且零依赖 |
| 邮件 | `nodemailer`（唯一依赖） | 没有它就得手写 SMTP，不划算；且**不配置也能跑**（降级为落盘） |
| 桌面通知 | PowerShell 调用系统 Toast API | 零依赖 |
| 定时 | 服务内 `setInterval` + 启动补跑 | 不依赖 Windows 计划任务，卸载干净 |

**唯一的外部依赖是 nodemailer，且是可选的。**

### 8.2 目录结构

```
workbuddy校园工作台/
├─ 方案文档.md            ← 本文档
├─ README.md              ← 怎么用
├─ start.bat              ← 双击启动
├─ server.js              ← 本地小服务入口
├─ package.json
├─ src/
│  ├─ store.js            ← 数据层：读写、UUID、软删除、变更日志、自动备份
│  ├─ library.js          ← 预置库：装载、版本、导入更新包、推荐排序
│  ├─ reminder.js         ← 提醒：每日扫描、7/3/1、补发、邮件、桌面通知
│  ├─ export.js           ← 导出：三口径 × 中英双语
│  └─ seed/
│     └─ competitions.json ← 84 项榜单骨架
├─ public/
│  ├─ index.html
│  ├─ styles.css          ← 设计令牌 + Bento + 微动效
│  └─ app.js              ← 视图与交互
├─ data/                  ← 你的数据（纯本地）
│  ├─ profile.json
│  ├─ experiences.json
│  ├─ library.json
│  ├─ changelog.jsonl
│  ├─ backups/            ← 滚动覆盖，只留最新一份
│  └─ outbox/             ← 未配置邮箱时的邮件落盘
└─ attachments/           ← 附件本体（复制进来，原文件删了也不丢）
```

### 8.3 数据安全

- 服务**只监听 `127.0.0.1`**，局域网内其他设备连不上，等于没有暴露面。
- 每次改动即写盘；写盘前先写备份，**滚动覆盖**（只保留最新一份）。
- 附件**复制**进 `attachments/`，不是记路径——原文件挪走或删掉都不影响。
- 目前**不做鉴权**（因为本机访问就是全部访问）；将来做手机端时**加中转层**，而不是直接开端口。

---

## 9. 实施阶段

| 阶段 | 内容 | 状态 |
|---|---|---|
| 1 | 服务骨架 + 数据层（读写/备份/变更日志） | 进行中 |
| 2 | 预置竞赛库（84 项骨架） | 待办 |
| 3 | 前端六个视图 + Bento 首页 + 微动效 | 待办 |
| 4 | 提醒模块（扫描/补发/邮件/桌面通知） | 待办 |
| 5 | 导出模块（三口径 × 双语） | 待办 |
| 6 | 视觉模板接入（等用户提供） | 待用户 |
| 7 | 手机端只读 + 单向同步 | 后期 |

---

## 10. 已知未决

1. **视觉模板**——用户提供后覆盖 §7.1 的基调。
2. **SMTP 配置**——需要用户提供邮箱与授权码；未提供则桌面通知 + `.eml` 落盘先跑起来。
3. **B 方案（云端代收）**——不做，接口预留。
4. **手机端**——后期，数据模型已预留 UUID / updatedAt / deletedAt / changelog。
5. **导出模板的具体排布**——先用通用排版，等你看过实际输出再调。
